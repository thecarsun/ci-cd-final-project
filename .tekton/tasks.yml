    apiVersion: tekton.dev/v1beta1
    kind: Task
    metadata:
      name: cleanup
    
    spec:
      description: This task will have a single step called remove as follows
      workspaces:
        - name: source     
      steps:
        - name: remove
          image: alpine:3
          env:
            - name: WORKSPACE_SOURCE_PATH
              value: $(workspaces.source.path)
          workingDir: $(workspaces.source.path)
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
          script: |
            #!/usr/bin/env sh
            set -eu
            echo "Removing all files from ${WORKSPACE_SOURCE_PATH} ..."
              # Delete any existing contents of the directory if it exists.
              #
              # We don't just "rm -rf ${WORKSPACE_SOURCE_PATH}" because ${WORKSPACE_SOURCE_PATH} might be "/"
              # or the root of a mounted volume.
              if [ -d "${WORKSPACE_SOURCE_PATH}" ] ; then
                # Delete non-hidden files and directories
                rm -rf "${WORKSPACE_SOURCE_PATH:?}"/*
                # Delete files and directories starting with . but excluding ..
                rm -rf "${WORKSPACE_SOURCE_PATH}"/.[!.]*
                # Delete files and directories starting with .. plus any other character
                rm -rf "${WORKSPACE_SOURCE_PATH}"/..?*
              fi

apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: nose
spec:
  description: Run unit tests using nose
  workspaces:
    - name: source
  params:
    - name: args
      description: Arguments to pass to nose
      type: string
      default: "-v"
  steps:
    - name: nosetests
      image: python:3.9-slim
      workingDir: $(workspaces.source.path)
      script: |
        #!/usr/bin/env sh
        set -eu
        python -m pip install --upgrade pip wheel
        pip install -r requirements.txt
        nosetests $(params.args)

apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: buildah
spec:
  spec:
  workspaces:
    - name: source
  params:
    - name: build-image
      type: string
      description: Image name to build
      default: my-app:latest
  steps:
    - name: build
      image: quay.io/buildah/stable:v1.30.0
      workingDir: $(workspaces.source.path)
      script: |
        #!/usr/bin/env sh
        set -eu
        echo "Building image $(params.build-image)"
        buildah bud -t $(params.build-image) 